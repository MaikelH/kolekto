import {assert as Assert} from "chai";

describe("Count-Min Sketch", () => {
	describe('Construction', () => {
		it('Should create Count-Min Sketch with fpp of 0.03 when initialized', () => {

		})
	});
});
